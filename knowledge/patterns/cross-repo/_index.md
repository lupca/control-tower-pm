# Cross-Repo Pattern Cache Index

Confirmed-useful cross-repo matches surfaced by `/pm`'s cross-repo search (`AGENTS.md` §14) and confirmed by the User ("yes, reuse this"). Checked before re-querying `cross_repo_search_tool` on a related keyword.

| Slug | Source project | Target project | Similarity | Created |
|:---|:---|:---|:---|:---|
| *(none yet)* | | | | |
