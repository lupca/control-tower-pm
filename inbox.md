# HÒM THƯ YÊU CẦU CHỜ PHÂN LOẠI (inbox.md)

Nơi lưu trữ các ghi chú thô, ý tưởng, hoặc feedback từ User. Gõ `/ingest` để Agent tự động phân loại những yêu cầu này thành các task chi tiết trong thư mục `projects/`.

---

## Ý TƯỞNG & YÊU CẦU MỚI:

1.  *Ghi chú (2026-07-21):* Cần làm thêm phần xuất hóa đơn VAT tự động khi khách hàng hoàn tất checkout trên hệ thống OMS. Cái này có vẻ sẽ đụng tới file `invoice_service.py` và cần viết thêm test trong `test_invoice.py`. Ưu tiên trung bình thôi, deadline khoảng 15-08-2026.
2.  *Ghi chú (2026-07-21):* Sửa lỗi variant không load được hình ảnh khi kích thước file ảnh quá lớn (lỗi nén ảnh phía frontend). Cái này ưu tiên cao, ảnh hưởng trực tiếp tới chuyển đổi bán hàng.
3.  *Ghi chú (2026-07-23):* [topvnsport-web] Tích hợp Zalo OA nhắn tin xác thực trước khi mua hàng. Gửi OTP/mã xác nhận qua Zalo OA cho khách hàng trên web storefront trước checkout. Chưa có tài liệu Zalo OA nào trong repo — cần research Zalo OA API docs (https://developers.zalo.me/docs). Ưu tiên trung bình.
4.  *Ghi chú (2026-07-23):* [control-tower-web] Lỗi giao diện dashboard: (a) Trang chi tiết một số project (vd: PMI) không lấy được data. (b) Kanban board không lấy được task của một số project. (c) Giao diện kanban bị kéo dài khi có nhiều task — cần giới hạn chiều cao cột và cho phép scroll (overflow-y).
5.  *Ghi chú (2026-07-23):* [control-tower] **IMPORTANT — ưu tiên cao.** Mở rộng /dispatch để hỗ trợ delegate task creation (không chỉ exec/review). Ý tưởng: viết 1 file handoff hướng dẫn agent (vd: gemini-3.1-pro) cách làm PM — đọc spec từ code repo, query graph, viết task file theo format control-tower. Flow: coordinator chỉ mô tả yêu cầu ngắn gọn → dispatch agent vào repo code (vd: topvnsport) → agent đọc codebase + graph → tự viết task file hoàn chỉnh (AC, files, tests, flows, predicted_success). Mục đích: tách coordinator (control-tower session) ra chỉ làm điều phối, không vừa research vừa tạo task (quá nặng context). Cần verify: ưu/nhược điểm, token cost, so sánh với flow hiện tại (/pm tự làm hết trong 1 session). Đây mới là ý tưởng, cần research trước khi implement.
6.  *Ghi chú (2026-07-23):* [control-tower] Quy tắc điều phối mới: khi executor đã làm lại lần 2 mà reviewer vẫn không pass → thay reviewer khác (không phải thay executor) để kiểm chứng lại xem lỗi có thật sự còn hay reviewer đánh giá sai. Nếu reviewer mới xác nhận hết lỗi → báo con người + update status task. Nếu reviewer mới xác nhận vẫn lỗi → escalate: thay executor bằng agent cao cấp hơn để fix. Cần implement vào flow /verdict hoặc /review-order — có thể track số lần changes-requested liên tiếp trong task frontmatter.
7.  *Ghi chú (2026-07-23):* [control-tower] Handoff tracking cho /dispatch. Hiện tại dispatch agent xong không biết agent đang làm gì, nghĩ gì, tiến độ ra sao. Giải pháp: khi dispatch, prompt agent phải viết/update một file handoff (vd: `projects/<name>/tasks/<ID>-handoff.md`) — ghi lại: (a) suy nghĩ/phân tích trước khi bắt tay làm, (b) tiến độ từng bước (đang làm AC nào, kết quả gì), (c) vấn đề gặp phải, quyết định đã chọn. Coordinator có thể đọc file này bất kỳ lúc nào để nắm tình hình mà không cần chờ agent xong. Cũng giúp debug khi agent fail — biết dừng ở đâu, tại sao.
8.  *Ghi chú (2026-07-23):* CD pipeline production deploy fail. Alembic migration `c9a2d4b80123` fail — `OCI runtime exec failed: exec failed: unable to start container process... found in $PATH: unknown`. Health checks [4/5] — `api-pmi` 200 OK nhưng `api-oms` 502. Process completed with exit code 1. Đây là nguyên nhân WMS-002 fix không được deploy lên prod (WEB-004). Project: topvnsport. Ưu tiên urgent.
